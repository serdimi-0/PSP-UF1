<UserControl x:Class="Client.MissatgeUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="ucRoot">
    <Grid Width="450">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Foreground="White" FontWeight="Black" Grid.Column="0" Text="{Binding ElementName=ucRoot, Path=myMessage.Usuari}"/>
        <TextBlock Foreground="White" Grid.Column="1" Text="{Binding ElementName=ucRoot, Path=myMessage.Text}"/>
        <TextBlock HorizontalAlignment="Right" Foreground="#B3B3B3" Grid.Column="2" Text="{Binding ElementName=ucRoot, Path=myMessage.HoraString}"/>
    </Grid>
</UserControl>
